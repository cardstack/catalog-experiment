{
  "name": "catalog-experiment",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "clean": "git clean -x -f",
    "build": "tsc",
    "test": "cd packages/builder && yarn test",
    "builder": "cd packages/builder && yarn start",
    "serve": "cd packages/app && yarn start",
    "test-server": "cd packages/builder && yarn start:test-file-daemon"
  },
  "resolutions": {
    "minimist": "^1.2.5"
  }
}