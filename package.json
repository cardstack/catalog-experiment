{
  "name": "catalog-experiment",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "init": "npm run build:web-streams-polyfill",
    "build": "tsc",
    "build:web-streams-polyfill": "cd ./node_modules/web-streams-polyfill && npm run build",
    "builder": "cd packages/builder && yarn start",
    "serve": "cd packages/app && yarn start"
  }
}